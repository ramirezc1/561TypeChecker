//y has to be initialized before use
//In execution, y will always be initialized before it is copied to x. It’s an error anyway!
class BadInit() {
def bad_init() {
rep = 0;
n_reps = 3;
while rep < n_reps {
if rep > 0 { 
x = y; // Skips first iteration
}
	rep = rep + 1; 
	y = 42; //Y is initialized here
} 
}
}