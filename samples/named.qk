//if two objects that are identical except for the name of their class, are they compatible?
//NO
//Quack types are named.
//They are never anonymous, and every named type is distinct.
class C(x: Int) { 
this.x = x; 
def val(): Int { return this.x } } 
class D(x: Int) 
{ this.x = x; 
def val(): Int { return this.x } } 
y = C(5);
z = D(5);
